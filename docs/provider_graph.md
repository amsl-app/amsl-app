# Provider dependency graph

Generated by `scripts/provider_graph.py` from the declared `@Riverpod(dependencies: [...])`. Do not edit by hand — re-run the script.

Arrows point from a provider to what it depends on. Amber = root (no deps); teal = the `HikariPod` hub.

```mermaid
flowchart LR
  classDef root fill:#fbedd8,stroke:#a85d0c,color:#a85d0c;
  classDef hub fill:#dcf1f1,stroke:#0e7c86,color:#0e7c86,font-weight:bold;
  AssessmentSessions --> HikariPod
  AsyncLoginNotifier --> storages
  ChatChannelNotifier --> HikariPod
  ChatChannelRepositoryNotifier --> ChatChannelNotifier
  ChatChannelRepositoryNotifier --> HikariPod
  ChatControllerNotifier --> ChatChannelNotifier
  ChatControllerNotifier --> ChatRepositoryNotifier
  ChatControllerNotifier --> ModuleNotifier
  ChatNotifier --> ChatControllerNotifier
  ChatRepositoryNotifier --> ChatChannelRepositoryNotifier
  ChatStreamNotifier --> ChatControllerNotifier
  Focus --> HikariPod
  HikariPod --> AsyncLoginNotifier
  HikariPod --> login
  HistoryProvider --> AssessmentSessions
  HistoryProvider --> HikariPod
  HistoryProvider --> ModuleNotifier
  IcalTokenProvider --> HikariPod
  Journal --> HikariPod
  Journal --> VariantPod
  ModuleConfigurationProvider --> HikariPod
  ModuleConfigurationProvider --> ModuleGroupsProvider
  ModuleConfigurationProvider --> ModuleNotifier
  ModuleGroupsProvider --> HikariPod
  ModuleNotifier --> AssessmentSessions
  ModuleNotifier --> HikariPod
  ModuleNotifier --> ModuleGroupsProvider
  PDFStore --> HikariPod
  PlannerProvider --> HikariPod
  Preferences --> storages
  QuizProvider --> HikariPod
  QuizScoreProvider --> HikariPod
  SessionPod --> ModuleNotifier
  UpdateData --> MinVersion
  UpdateData --> appVersion
  UserPod --> HikariPod
  UserPod --> storages
  VariantPod --> UserPod
  appVersion --> packageInfo
  login --> AsyncLoginNotifier
  tools --> ModuleConfigurationProvider
  tools --> VariantPod
  class MinVersion root;
  class packageInfo root;
  class storages root;
  class HikariPod hub;
```
